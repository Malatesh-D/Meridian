import './globals.css'

export const metadata = { title: 'MERIDIAN | Emergency Operations', description: 'Global energy network recovery operations' }

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>
}
